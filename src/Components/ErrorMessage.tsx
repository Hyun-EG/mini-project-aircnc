import styled from 'styled-components';

const ErrorMessage = styled.p`
  margin: 0;
  margin-top: 8px;
  padding: 0;
  font-size: 14px;
  text-align: right;
  color: red;
`;

export default ErrorMessage;
